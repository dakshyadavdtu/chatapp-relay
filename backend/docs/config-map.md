# Config map (baseline)

Baseline placeholder. Runtime configuration is loaded from `config/env.js` after `config/env.validate.js` runs.

See `docs/` at repository root for deployment and security notes.
