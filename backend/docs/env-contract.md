# Environment contract (baseline)

This file exists for `scripts/verify-baseline.js` (Phase 0). Authoritative env documentation lives in the repository root `docs/` tree (for example `docs/config/ENV_TEMPLATE.md` and `backend/.env.example`).

Do not duplicate secret values here.
