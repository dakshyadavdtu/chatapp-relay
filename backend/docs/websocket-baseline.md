# WebSocket baseline (baseline)

Placeholder for Phase 0 baseline checks. Protocol types and flows are defined in `backend/CONTRACT.json` and exercised by `backend/tests/` and `frontend/src/contracts/coverage.json`.
